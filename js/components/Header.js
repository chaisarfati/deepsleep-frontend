/**
 * Header.js — In the new design the topbar has been removed.
 * Navigation and user controls are in the sidebar.
 * This stub exists for backward compatibility with app.js imports.
 */

export function renderHeader() {
  return "";
}
